import 'package:flutter/material.dart';

class MonthTodoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Month')),
    );
  }
}
