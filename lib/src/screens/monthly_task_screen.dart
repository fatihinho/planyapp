import 'package:flutter/material.dart';

class MonthlyTaskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Month')),
    );
  }
}
